repos:
  - repo: local
    hooks:
      - id: bump-version
        name: bump version (YYYY.MM.PATCH)
        entry: pixi run python scripts/bump_version.py
        language: system
        pass_filenames: false
