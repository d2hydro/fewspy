[workspace]
name = "fewspy"
description = "A Python API for the Deltares FEWS PI REST Web Service"
channels = ["conda-forge"]
platforms = ["win-64", "linux-64"]
readme = "README.md"
license = "MIT"
license-file = "LICENSE"

[tasks]
tests = "pytest --cov=fewspy --cov-report=term-missing ./tests"
build = "python -m build"
lint = "pre-commit run --all-files"

[dependencies]
python = "3.14.*"
pre-commit = "*"
ruff = ">=0.12"
aiohttp = "*"
geopandas = "*"
ipykernel = "*"
lxml = "*"
pandas = "*"
pyarrow = "*"
pydantic="*"
pytest = ">=9.0.3,<10"
pytest-cov = "*"
pytest-flakes = "*"
netCDF4 = "*"
xarray="*"

[pypi-dependencies]
fewspy = { path = ".", editable = true }
build = "*"
pydantic = "*"
